// Adresse de ton API Go. Si le frontend est servi par le même serveur Go
// (via /app/ comme indiqué dans README_INTEGRATION.md), laisser une chaîne vide.
const API_BASE = "http://api.snase.org:8080";
